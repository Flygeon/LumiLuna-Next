# Changelog

## 1.1.0 (2026-08-18)

### 新增

- **桌面歌词窗口**：独立透明置顶窗口，显示当前歌词；支持点击展开/自动隐藏控制栏，右击可切换显示效果（淡入/上滑/缩放/模糊浮现），设置中可调节字号、不透明度、锁定位置等。
- **百宝箱**：侧边栏新增「百宝箱」选项卡，收纳 WebDAV 远程媒体入口与音效预设市场。
- **音效预设市场**：在线浏览和下载来自 GitHub 仓库（Flygeon/LumiLuna-Presets）的音效预设，一键导入。
- **预设分享码偏好**：设置中可控制分享码输出格式（仅中文/仅原版/两者同时输出），导出时自动带标签说明。
- **预设分享码压缩**：LLFX3 紧凑二进制格式，大幅缩短分享码长度（~44 字符），支持 LLFX1 旧格式导入。
- **7537 字库字符码分享**：支持中文预设名称 @ 字符码的分享格式，内置动漫/音游彩蛋预设。
- 音效预设仓库 CI（`Flygeon/LumiLuna-Presets`）：每小时自动更新索引，支持手动触发，预设文件变更时自动重建。

### 优化

- 桌面歌词控制栏交互重做：点击歌词展开，5 秒无操作自动隐藏，设置可选始终显示。
- 桌面歌词窗口边界阴影：展开控制栏时显示窗口轮廓，便于识别窗口大小。
- 桌面歌词支持双击播放/暂停，设置中可选。
- 桌面歌词默认不显示下一句，设置中可开启。

### 修复

- 桌面歌词窗口无法拖动 bug（恢复整窗拖拽，双击拦截最大化）。
- 桌面歌词右键菜单被窗口截断（已移除右键菜单，改为设置页调节）。
- 桌面歌词顶部控制栏遮挡歌词（展开时歌词区自动下移）。
- 鼠标移出桌面歌词控制栏不隐藏（改用延迟隐藏 + 窗口失焦监听）。
- 百宝箱页面 i18n 键名错误导致不显示中文。

### 技术

- 升级 Tauri 2 窗口/事件 API 使用。
- 新增桌面歌词事件同步机制（主窗口 → 子窗口）。
- 预设仓库独立 GitHub 仓库，JSON 格式索引，CI 自动生成。